# Kernel configuration fragments

Architecture-specific Linux kernel configuration fragments will live here. Initial targets mirror `architecture/KERNEL.md`:

- `x86_64/` for day-one x86-64 builds
- `aarch64/` for day-one ARM64 builds
- `riscv64/` for planned RISC-V builds
